var a = 0;
var b = 1;
var b2 = -1;
var c = false;
var d = null;
var e;
var f = 'un string';
var g = {};
var h = [];

if(a){ console.log('a est evalué à true') } else { console.log('a est evalué à false') };
if(b){ console.log('b est evalué à true') } else { console.log('b est evalué à false') };
if(b2){ console.log('b2 est evalué à true') } else { console.log('b2 est evalué à false')};
if(c){ console.log('c est evalué à true') } else { console.log('c est evalué à false')};
if(d){ console.log('d est evalué à true') } else { console.log('d est evalué à false')};
if(e){ console.log('e est evalué à true') } else { console.log('e est evalué à false')};
if(f){ console.log('f est evalué à true') } else { console.log('f est evalué à false')};
if(g){ console.log('g est evalué à true') } else { console.log('g est evalué à false')};
if(h){ console.log('h est evalué à true') } else { console.log('h est evalué à false')};
    

